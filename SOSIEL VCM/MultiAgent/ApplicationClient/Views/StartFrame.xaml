<Page x:Class="ApplicationClient.StartFrame"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ignore="http://www.galasoft.ch/ignore"
      xmlns:applicationClient="clr-namespace:ApplicationClient"
      xmlns:chartSurfaceControl="clr-namespace:ChartSurfaceControl;assembly=ChartSurfaceControl"
      mc:Ignorable="d ignore"
      Title="MVVM Light Application"
      DataContext="{Binding Main, Source={StaticResource Locator}}">

    <Grid x:Name="LayoutRoot">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Vertical"
                    Margin="15" Grid.Column="0">

            <StackPanel>
                <Label Content="Iterations Number:"
                       Margin="5" />
                <TextBox Text="{Binding Iterations}" Margin="5,5,420,5" />
            </StackPanel>
            <StackPanel>
                <Label Content="Agents Number:"
                       Margin="5" />
                <TextBox Text="{Binding NParameter}" Margin="358,5,5,5" />
            </StackPanel>
            <StackPanel>
                <Label Content="M Parameter:"
                       Margin="5" />
                <TextBox Text="{Binding MParameter}" Margin="5" />
            </StackPanel>

            <Button Margin="5" Content="Strategies" />
            <Button Margin="5" Content="Agents" Command="{Binding AgentsCommand}" />
            <Button Margin="5" Content="Communication Map" Command="{Binding CommunicationMapCommand}" />
            <Button Content="Open Conf file" Command="{Binding SetConfigurationFilePathCommand}" Margin="5" />


        </StackPanel>
        <StackPanel Grid.Column="1">
            <chartSurfaceControl:ChartSurfaceControl Width="500" Height="400" LineData="{Binding Contributions, Mode=TwoWay}">
            </chartSurfaceControl:ChartSurfaceControl>
            <!--<ListBox ItemsSource="{Binding Updates}"
                     Margin="5"
                     MaxHeight="350"
                     ScrollViewer.VerticalScrollBarVisibility="Visible" Height="113" />-->
            <StackPanel Orientation="Horizontal">
                <Button Content="Run one iteration" Command="{Binding RunOnceCommand}" Margin="5" />
                <Button Content="Run all iterations" Command="{Binding RunAllCommand}" Margin="5" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>